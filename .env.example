# Example environment variables for Guide Voyage
# Copy this file to `.env` and fill the values before running the project.

# Path to Firebase Admin service account JSON (used by scripts in `scripts/`)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Firebase web config (Vite expects env vars prefixed with VITE_)
VITE_FIREBASE_API_KEY=your-web-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-auth-domain
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-storage-bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Optional: Google Maps API key (used in map components)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Node environment
NODE_ENV=development

# Notes:
# - Do NOT commit your real `.env` to version control. Add it to `.gitignore`.
# - `FIREBASE_SERVICE_ACCOUNT_PATH` can be an absolute path to the JSON file
#   or a relative path like `./firebase-service-account.json`.
